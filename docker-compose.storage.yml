version: '3.9'
services:
#   jaeger:
#     image: jaegertracing/opentelemetry-all-in-one:latest
#     networks:
#       - notify-server
#     ports:
#       - "3001:16686"

  postgres:
    image: postgres:16
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
    - "5432:5432"

networks:
  notify-server:
    ipam:
      driver: default
